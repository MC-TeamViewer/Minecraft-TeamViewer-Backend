[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

# 控制台输出
addopts = -v --tb=short

# 标记
markers =
    integration: 集成测试 (需要运行中的服务端)
